<Window x:Class="task3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:task3"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <ComboBox Grid.Row="0" Margin="50" HorizontalAlignment="Stretch" x:Name="cmbb" >
            <ComboBoxItem Tag="Red">
                <Label>Red</Label>
            </ComboBoxItem>
            <ComboBoxItem Tag="Blue">
                <Label>Blue</Label>
            </ComboBoxItem>
        </ComboBox>
        <TextBox Grid.Row="1" x:Name="text1" Margin="50" HorizontalAlignment="Stretch" Height="30"></TextBox>
        <TextBlock Grid.Row="2" Margin="50" HorizontalAlignment="Stretch" 
                Text="{Binding ElementName=text1, Path=Text}"
                Foreground="{Binding ElementName=cmbb, Path=SelectedItem.Tag}">
        </TextBlock>
    </Grid>
</Window>
